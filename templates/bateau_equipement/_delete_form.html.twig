<form method="post" action="{{ path('app_bateau_equipement_delete', {'id': bateau_equipement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bateau_equipement.id) }}">
    <button class="btn">Delete</button>
</form>
